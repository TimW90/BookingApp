import Accordion from '../common/Accordion';
import useHotels from '@/hooks/useHotels';

const HotelListNew = ({ isAdmin }) => {
  const hotels = useHotels();

  return <Accordion items={hotels} />;
};

export default HotelListNew;
